# echo -n "valor" | base64
apiVersion: v1
kind: Secret
metadata:
  name: evaluation-service-secret
  namespace: togglemaster
type: Opaque
data:
  # echo -n "redis://<elasticache-endpoint>:6379" | base64
  REDIS_URL: SUBSTITUIR_BASE64
  # echo -n "<chave-de-api-criada-no-auth-service>" | base64
  SERVICE_API_KEY: SUBSTITUIR_BASE64
  # echo -n "https://sqs.<region>.amazonaws.com/<account-id>/togglemaster-events" | base64
  AWS_SQS_URL: SUBSTITUIR_BASE64
