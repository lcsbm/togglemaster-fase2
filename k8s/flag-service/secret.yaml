# echo -n "valor" | base64
apiVersion: v1
kind: Secret
metadata:
  name: flag-service-secret
  namespace: togglemaster
type: Opaque
data:
  # echo -n "postgres://<user>:<pass>@<rds-flags-endpoint>:5432/flags_db?sslmode=require" | base64
  DATABASE_URL: SUBSTITUIR_BASE64
