# echo -n "valor" | base64
apiVersion: v1
kind: Secret
metadata:
  name: targeting-service-secret
  namespace: togglemaster
type: Opaque
data:
  # echo -n "postgres://<user>:<pass>@<rds-targeting-endpoint>:5432/targeting_db?sslmode=require" | base64
  DATABASE_URL: SUBSTITUIR_BASE64
